<?php
/**
 * @file
 * Code for the Landportal field display feature.
 */

include_once 'landportal_display.features.inc';

/******************************************
 *
 *    Hacks for drupal node/page title
 */

function landportal_display_preprocess_page(&$variables) {
  // Taxonomy, clear title if we have a name_field
  if (@($term = $variables['page']['content']['system_main']['term_heading']['term']) &&
      isset($term['name_field'])) {
    $variables['title'] = '';
    //dpm($term, "Got a term");
  }
  if (isset($variables['node']) && isset($variables['node']->title_field) &&
      array_key_exists('nodes', $variables['page']['content']['system_main'])) {
    landportal_display__get_group_header($variables['node']->nid, $variables);
  }
}

//
// Pick group_header and create a new group (title_header) to render instead of title
//
function landportal_display__get_group_header($nid, &$variables) {
    $content = $variables['page']['content']['system_main']['nodes'][$nid];
    if (isset($content['#fieldgroups']) && isset($content['#fieldgroups']['group_header'])) {
      $header = $content['#fieldgroups']['group_header'];
      $title = landportal_display_extract_groups('group_header', $content);
      field_group_fields_nest($title, $content);
      $variables['page']['title_header'] = $title;
      $variables['title'] = render($title);
      unset($variables['page']['content']['system_main']['nodes'][$nid]['#fieldgroups']['group_header']);
      unset($variables['page']['content']['system_main']['nodes'][$nid]['#groups']['group_header']);
      // Remove fields and groups children that now belong to the page-header
      foreach ($title['#group_children'] as $c => $g) {
        unset($variables['page']['content']['system_main']['nodes'][$nid][$c]);
        unset($variables['page']['content']['system_main']['nodes'][$nid]['#group_children'][$c]);
      }
      //dpm($variables['page']['content']['system_main']['nodes'][$nid], 'da node afta');
    }
}

function landportal_display_extract_groups($group_name, $content) {
  static $i = 0;
  $g = array(
    '#fieldgroups' => [],
    '#group_children' => [],
  );
  foreach ($content['#groups'][$group_name]->children as $field) {
    // Process nested groups
    if (substr($field, 0, 6) == 'group_') {
      $i++;
      $cg = landportal_display_extract_groups($field, $content);
      $i--;
      $g['#fieldgroups'][$field] = $cg['#fieldgroups'][$field];
      $g['#group_children'][$field] = $group_name;
      $g['#group_children'] += $cg['#group_children'];
      $g['#items'] += $cg['#items'];
      //unset($variables['page']['content']['system_main']['nodes'][$node->nid][$fieldc]);
    } else {
      if (isset($content[$field])) {
        $g['#items'][$field] = $content[$field];
        $g['#group_children'][$field] = $group_name;
      }
      //unset($variables['page']['content']['system_main']['nodes'][$node->nid][$field]);
    }
  }
  //dpm($g, 'E: '.$group_name);
  $g['#fieldgroups'][$group_name] = $content['#groups'][$group_name];
  $g += $g['#items'];
  if (!$i) unset($g['#items']);
  return $g;
}


/******************************************
 *
 *    Hacks for select2 on forms
 */

/**********************
** Implementation of hook_entity_info_alter().
***********************/

function landportal_display_entity_info_alter(&$entity_info){
  $entity_info['node']['view modes']['listing'] = array(
    'label' => t('Listing'),
    'custom settings' => TRUE,
  );
}

function landportal_display_form_alter(&$form, &$form_state, $form_id) {
  $fields = array(
    // LL specific
    'field_doc_type',
    'field_doc_publisher',
    'field_doc_provider',
    // LB specific
    'field_indicators',
    'field_indicator_ranking',
    'field_indicator_map',
    /* // Admin views */
    'type', // CT type? // for view admin
    'field_doc_type_tid',
    'field_doc_publisher_tid',
    'field_doc_provider_tid',
    // Commons
    'field_featured',
    'field_orgref',
    'field_geographical_focus',
    'field_related_topics',
    'field_related_domains',
    'field_related_themes'
  );
  $select = array();
  // Quickly scan form for fields
  foreach($fields as $field) {
    if (array_key_exists($field, $form)) {
      $select[] = $field;
    }
  }

    // Do we have any matches (ie select field that uses jquery.select) ?
  $forms = array(
    'field_ui_field_overview_form', 'user_profile_form', 'user_register_form',
    'apachesolr_search_custom_page_search_form', 'views_exposed_form',
    'taxonomy-form-term'
  );
  if (count($select) || in_array($form_id, $forms) || preg_match('/^views_ui_/', $form_id)) {
    // dpm($select, 'selected fields');
    global $theme;
    if ($theme != 'lpbs') {
      $form['#attached']['css'][] = drupal_get_path('module', 'landportal_display') . '/select2.css';
    }
    //$form['#attached']['js'][] = drupal_get_path('theme', 'lpbs') . '/js/select2.min.js';
    $form['#attached']['js'][] = drupal_get_path('module', 'landportal_display') . '/select2.min.js';
    $form['#attached']['js'][] = drupal_get_path('module', 'landportal_display') . '/select.js';
  }
}
